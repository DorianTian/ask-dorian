[package]
name = "ask-dorian-desktop"
version = "0.1.0"
description = "Ask Dorian Desktop — Tauri v2 menubar app"
edition = "2021"

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
tauri-build = { version = "2", features = [] }
